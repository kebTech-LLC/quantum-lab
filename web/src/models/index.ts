/**
 * Models: classes wrapping data with computed getters and behavior methods.
 * Pattern (established in cnctd.world): a plain interface for the data
 * contract, a class wrapper for behavior, a static fromPlainObject factory.
 * First real models arrive with Phase 1 (e.g. a QubitState wrapping
 * amplitudes with getters for Bloch coordinates and probabilities).
 */
export {};
