{
  "name": "quantum-lab-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "wasm-build": "wasm-pack build ../crates/qsv-wasm --target web --out-dir ../../web/src/modules/sim/pkg --out-name qsv_wasm",
    "dev": "npm run wasm-build && vite",
    "build": "npm run wasm-build && vue-tsc && vite build",
    "typecheck": "vue-tsc",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.40"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "typescript": "^5.9.3",
    "vite": "^8.1.5",
    "vue-tsc": "^3.3.7",
    "wasm-pack": "^0.15.0"
  }
}
